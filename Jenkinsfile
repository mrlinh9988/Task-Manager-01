pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        echo 'Building the application...'
      }
    }

    stage('deploy') {
      steps {
        echo 'Deploy application'
      }
    }
  }
}
