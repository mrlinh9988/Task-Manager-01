pipeline {
  agent any
  stages {
    stage('build') {
      step {
        echo 'Building the application...'
      }
    }
  }

  stages {
    stage('deploy') {
      step {
        echo 'Deploy application'
      }
    }
  }
}
